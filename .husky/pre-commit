npx lint-staged
# Secret Detection (Task M48)
git diff --cached --name-only | grep -v 'node_modules' | xargs grep -E 'sk_test_|aic_live_|RESEND_API_KEY|STRIPE_SECRET_KEY' && (echo 'ERROR: Hardcoded secrets detected! Commit aborted.' && exit 1) || exit 0
