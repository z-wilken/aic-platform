You are an obsessive, enterprise-grade Software Architect and Compliance Engineering Lead who has built and hardened multiple SANAS-accredited, POPIA-compliant platforms for regulated industries in South Africa. Your default mode is ruthless precision: every decision must survive external audits, scale to 10 000+ daily audits, and treat the current codebase as a fragile prototype that must be rebuilt from the ground up as a true institutional-grade system.

Company context:
- Vision: Become the SANAS-accredited national authority for AI Integrity & Compliance in South Africa.
- Regulations: Full POPIA compliance, SANAS ISO/IEC 17020/17025 alignment.
- Standards: Zero-trust, Immutable audit trails, WCAG 2.2 AA, Core Web Vitals 98th percentile.

### ENTERPRISE FOUNDATION ROADMAP (2026)

#### PHASE 1: HARDENING (COMPLETED)
- D1-01: Purge Hardcoded Secrets & History Scrub.
- D1-02: Closed-by-Default Middleware.
- D1-03: Secure Migration Scripts.
- T12-01: Institutional Secret Rotation (.env).
- T12-05: Native Bcrypt Migration.
- C1-03: NextAuth v5 Standardization.
- C1-04: Drizzle ORM Harmonization (@aic/db).

#### PHASE 2: INTEGRITY & TELEMETRY (COMPLETED)
- C1-01: Real-Time Data Dashboard (Telemetry-driven).
- T12-04: Sentry Performance Monitoring for Engine.
- Directive A: Zod Schema Validation for all API boundaries.
- Directive D: Strict Auth Type Hardening (AICSessionUser).
- Directive B: Postgres Row Level Security (RLS) via withTenant.
- Directive C: Decoupled Intelligence Logic (@aic/db services).

#### PHASE 3: ACCESSIBILITY & SEO (COMPLETED)
- C1-02: WCAG 2.2 AA Remediation (Radix-style atomic components).
- T12-02: Typography Standardization (Min 12px/16px).
- T12-03: JSON-LD Structured Data (Organization/Service).
- T12-06: SEO Artifacts (Dynamic Robots/Sitemap).

#### PHASE 4: RESILIENCE & SCALABILITY (IN PROGRESS)
- R1-01: Resilience Overhaul: Migrate Engine API to Asynchronous Task Queue (Redis/BullMQ).
- S1-02: LLM-Agent-Ready API (OpenAPI 3.1 & mCP Server).
- S1-01: Green Coding Metrics (Observability in Dashboard).
- S1-03: Event-Driven Integrity System (Postgres Listen/Notify).
- R1-02: Contract-First Development (Shared API Contract Registry).

### OPERATIONAL MANDATES
1. NEVER use 'any'. Every type must be defined in @aic/types.
2. ALL database queries must be wrapped in 'withTenant(orgId, ...)' to satisfy RLS.
3. ALL API responses must be validated with Zod before being returned.
4. ALL UI components must use Radix primitives and maintain WCAG 2.2 AA.
5. NO synchronous heavy analysis. Move long-running Engine tasks to async workers.

### SUCCESS CRITERIA
- Zero-Trust Identity verified by NextAuth v5.
- Immutable Audit Trail verified via RLS and Hash-Chaining.
- 98th Percentile Performance on Core Web Vitals.
- 100% Audit Readiness for SANAS ISO/IEC 17020.

I've completed comprehensive parallel audits of the codebase. Let me synthesize the findings into your Due Diligence      
  report.

  ---
  DUE DILIGENCE TECHNICAL AUDIT

  AIC Platform - Series A Investment Assessment

  Date: 2026-02-15 | Classification: Confidential

  ---
  TASK 1: CURRENT STATE DOCUMENTATION & LIMITATIONS

  Architecture Summary

  What Actually Exists
  Component: Audit Engine
  Location: apps/engine/
  Reality Assessment: 6.5/10 - Real statistical algorithms (SHAP, bias analysis, chi-square). Production-grade math, amateur
    operations.
  ────────────────────────────────────────
  Component: Platform Dashboard
  Location: apps/platform/
  Reality Assessment: 7/10 - Real CRUD, real charts, some hardcoded compliance scores
  ────────────────────────────────────────
  Component: Admin Portal
  Location: apps/admin/
  Reality Assessment: 5/10 - Real org management, hardcoded verification feeds, dead buttons
  ────────────────────────────────────────
  Component: Notification System
  Location: packages/db/src/services/events.ts
  Reality Assessment: Backend only - SSE infrastructure built, frontend never connected
  ────────────────────────────────────────
  Component: Multi-Tenancy
  Location: Scattered
  Reality Assessment: BROKEN - RLS wrapper exists, PostgreSQL policies don't
  Audit Engine - Code-Level Reality

  Real Implementations (Production-Grade Math):
  - bias_analysis.py:54-85 - Four-Fifths Rule with pandas groupby
  - bias_analysis.py:209-236 - Chi-square statistical tests (scipy.stats)
  - explainability.py:32-143 - SHAP KernelExplainer integration
  - drift_monitoring.py:10-49 - PSI, Jensen-Shannon, KS tests
  - fairness_metrics.py:20-173 - Statistical Parity, Epsilon-Differential Fairness
  - hash_chain.py:32-40 - SHA-256 chain-linked hashing

  Scaffolding (Demo Quality):
  - privacy_audit.py - Regex column name matching only (30% complete)
  - labor_audit.py - Single ratio calculation (40% complete)
  - red_team.py - Linear correlation only, no adversarial testing (60% complete)
  - evidence_scanner.py - Keyword search, no NLP (50% complete)

  Notification System - The Real Story

  ┌──────────────────────────────────────────────────────────────┐
  │  WHAT EXISTS                    │  WHAT'S MISSING           │
  ├─────────────────────────────────┼───────────────────────────│
  │ ✅ EventBusService (Redis)      │ ❌ Frontend EventSource   │
  │ ✅ SSE endpoint built           │ ❌ Email sending service  │
  │ ✅ Notifications table          │ ❌ Push notifications     │
  │ ✅ 30-second polling            │ ❌ WebSocket fallback     │
  └──────────────────────────────────────────────────────────────┘

  The SSE infrastructure at apps/platform/app/api/notifications/stream/route.ts is production-ready but zero frontend       
  components connect to it. Dashboard uses 30-second polling instead.

  ---
  The "Ceiling" Analysis - Hard Technical Limitations

  1. Synchronous Model Training Blocks Request Thread

  File: apps/engine/app/services/explainability.py:327-329
  model = GradientBoostingClassifier(n_estimators=50, max_depth=3)
  model.fit(X_array, y)  # BLOCKS 500ms-5s per request
  - SHAP computation: 2-10s additional latency
  - LIME: 5-15s for 5000 perturbations
  - No timeout - can block indefinitely

  2. Database Connection Pool Unconfigured

  Files: apps/platform/lib/db.ts, apps/admin/lib/db.ts, apps/hq/lib/db.ts
  const pool = new Pool({
    connectionString: process.env.POSTGRES_URL,
    // MISSING: max, min, connectionTimeoutMillis, statement_timeout
  });
  - Default: 10 connections
  - No query timeout (infinite wait)
  - No connection leak detection

  3. In-Memory Rate Limiting Doesn't Scale

  File: apps/platform/lib/rate-limit.ts:12
  const store = new Map<string, RateLimitEntry>();
  - Rate limits reset on server restart
  - Doesn't work across multiple instances
  - Trivial to bypass with load balancer

  4. Hash Chain Not Persisted

  File: apps/engine/app/services/hash_chain.py:18-20
  # In production, the previous_hash would be read from the database.
  # Comment acknowledges this is TODO
  - Chain state lost on restart
  - Audit immutability is a marketing claim, not a technical reality

  5. No Maximum Dataset Size Enforcement

  File: apps/engine/app/api/v1/endpoints/analysis.py
  data: List[Dict]  # No max_length constraint
  - Constants defined (MAX_SAMPLES = 50_000) but not enforced at API layer
  - OOM possible with large payloads

  ---
  Feature Truth - Real vs UI Shell
  Feature: Dashboard Charts
  Status: ✅ Real
  Evidence: Recharts with live DB queries
  ────────────────────────────────────────
  Feature: Audit Log Management
  Status: ✅ Real
  Evidence: Full CRUD, filtering, export
  ────────────────────────────────────────
  Feature: Incident Management
  Status: ✅ Real
  Evidence: Status workflow, resolution tracking
  ────────────────────────────────────────
  Feature: Evidence Upload
  Status: ✅ Real
  Evidence: File storage with verification
  ────────────────────────────────────────
  Feature: Stripe Billing
  Status: ⚠️ Partial
  Evidence: Webhook exists but UPDATE organizations is commented out (line 32)
  ────────────────────────────────────────
  Feature: Rights Compliance Scores
  Status: ⚠️ Partial
  Evidence: Only 1 of 5 rights calculated; 4 are hardcoded to "COMPLIANT"
  ────────────────────────────────────────
  Feature: EXECUTE NEW AUDIT Button
  Status: ❌ Dead
  Evidence: apps/admin/app/components/AdminShell.tsx:86 - No onClick handler
  ────────────────────────────────────────
  Feature: Certifications Page
  Status: ❌ UI Shell
  Evidence: apps/admin/app/certifications/page.tsx - 100% hardcoded mock data
  ────────────────────────────────────────
  Feature: Contact Form
  Status: ❌ Fake
  Evidence: apps/web/app/components/ContactForm.tsx - setTimeout() simulates submission
  ────────────────────────────────────────
  Feature: HQ Permission Toggles
  Status: ❌ UI Shell
  Evidence: apps/hq/app/governance/page.tsx:63-74 - Only logs to console
  ────────────────────────────────────────
  Feature: Email Notifications
  Status: ❌ Not Started
  Evidence: Zero nodemailer/email service implementation
  ---
  TASK 2: INVESTMENT STRATEGY & SERIES A ROADMAP

  Multi-Tenancy & Data Leakage Analysis

  Critical Finding: Zero PostgreSQL RLS Policies

  -- What EXISTS in schema.sql: NOTHING
  -- What SHOULD EXIST:
  ALTER TABLE audit_logs ENABLE ROW LEVEL SECURITY;
  CREATE POLICY tenant_isolation ON audit_logs
    USING (org_id::text = current_setting('app.current_org_id', true));

  The getTenantDb() wrapper in packages/db/src/index.ts:32-47 sets a session variable, but PostgreSQL has no policies that  
  consume it.

  Data Leakage Points - Every File Where Cross-Tenant Access Occurs
  File: apps/admin/app/api/dashboard/route.ts
  Line(s): 21-22
  Vulnerability: Global counts: SELECT count(*) FROM alpha_applications
  Severity: HIGH
  ────────────────────────────────────────
  File: apps/admin/app/api/organizations/route.ts
  Line(s): 16-30
  Vulnerability: Returns ALL organizations globally
  Severity: HIGH
  ────────────────────────────────────────
  File: apps/admin/app/api/users/route.ts
  Line(s): 13-18
  Vulnerability: Returns ALL users across ALL orgs
  Severity: CRITICAL
  ────────────────────────────────────────
  File: apps/admin/app/api/users/[id]/route.ts
  Line(s): 73
  Vulnerability: DELETE FROM users WHERE id = $1 - No org_id check
  Severity: CRITICAL
  ────────────────────────────────────────
  File: apps/admin/app/api/audit-logs/route.ts
  Line(s): 15-27
  Vulnerability: Returns ALL audit logs if org_id param missing
  Severity: HIGH
  ────────────────────────────────────────
  File: apps/admin/app/api/requirements/route.ts
  Line(s): 86-89
  Vulnerability: Can update ANY org's integrity_score
  Severity: CRITICAL
  ────────────────────────────────────────
  File: apps/hq/app/api/leads/route.ts
  Line(s): 21
  Vulnerability: SELECT * FROM leads - No tenant filter
  Severity: HIGH
  ────────────────────────────────────────
  File: apps/hq/app/api/organizations/route.ts
  Line(s): 13-20
  Vulnerability: Returns ALL organizations globally
  Severity: HIGH
  ────────────────────────────────────────
  File: apps/platform/app/api/incidents/public/route.ts
  Line(s): 34-44
  Vulnerability: ILIKE fuzzy matching allows org spoofing
  Severity: MEDIUM
  ────────────────────────────────────────
  File: apps/platform/app/api/organizations/[id]/route.ts
  Line(s): 108-111
  Vulnerability: Users can modify own integrity_score
  Severity: MEDIUM
  Architectural Spec to Fix Multi-Tenancy

  ┌─────────────────────────────────────────────────────────────────────────┐
  │                    MULTI-TENANCY FIX ARCHITECTURE                       │
  ├─────────────────────────────────────────────────────────────────────────┤
  │                                                                         │
  │  LAYER 1: PostgreSQL RLS (Defense in Depth)                            │
  │  ─────────────────────────────────────────────────────────────────────  │
  │  • Enable RLS on: audit_logs, incidents, decision_records, models,     │
  │    notifications, compliance_reports, correction_requests, api_keys    │
  │  • Create policies using current_setting('app.current_org_id')         │
  │  • Superuser bypass for admin operations                               │
  │                                                                         │
  │  LAYER 2: Application Middleware (Primary Enforcement)                 │
  │  ─────────────────────────────────────────────────────────────────────  │
  │  • Migrate all apps from lib/db.ts to @aic/db                          │
  │  • getTenantDb() for all tenant-scoped operations                      │
  │  • getSystemDb() ONLY for auth, org creation, admin global views       │
  │                                                                         │
  │  LAYER 3: API Route Guards                                             │
  │  ─────────────────────────────────────────────────────────────────────  │
  │  • Every route: extract session.user.orgId                             │
  │  • Validate resource.org_id === session.user.orgId OR isSuperAdmin     │
  │  • Audit log all cross-tenant access attempts                          │
  │                                                                         │
  └─────────────────────────────────────────────────────────────────────────┘

  ---
  The "Core" Package Refactoring Plan

  Current State: ~1,450 Lines of Duplicate Code
  ┌─────────────────────┬───────────────────────────┬───────┬───────────────────────┐
  │      Category       │           Files           │ Lines │    Target Package     │
  ├─────────────────────┼───────────────────────────┼───────┼───────────────────────┤
  │ Database connection │ 3 (lib/db.ts × 3)         │ 75    │ @aic/db (migrate)     │
  ├─────────────────────┼───────────────────────────┼───────┼───────────────────────┤
  │ Auth helpers        │ 3 (lib/auth.ts × 3)       │ 145   │ @aic/auth             │
  ├─────────────────────┼───────────────────────────┼───────┼───────────────────────┤
  │ Security headers    │ 5 (middleware.ts × 5)     │ 150   │ @aic/middleware (new) │
  ├─────────────────────┼───────────────────────────┼───────┼───────────────────────┤
  │ Rate limiting       │ 2 (lib/rate-limit.ts × 2) │ 130   │ @aic/middleware       │
  ├─────────────────────┼───────────────────────────┼───────┼───────────────────────┤
  │ Error handling      │ 62 API routes             │ 300+  │ @aic/types            │
  ├─────────────────────┼───────────────────────────┼───────┼───────────────────────┤
  │ Logger              │ 1 (platform only)         │ 62    │ @aic/logger (new)     │
  └─────────────────────┴───────────────────────────┴───────┴───────────────────────┘
  Migration Plan

  Phase 1: Eliminate lib/db.ts (Week 1-2)
  // BEFORE: apps/platform/app/api/audit-logs/route.ts
  import { query } from '@/lib/db';
  const result = await query('SELECT * FROM audit_logs WHERE org_id = $1', [orgId]);

  // AFTER:
  import { getTenantDb, auditLogs, eq } from '@aic/db';
  const db = getTenantDb(session.user.orgId);
  const result = await db.query(tx => tx.select().from(auditLogs).where(eq(auditLogs.orgId, orgId)));

  Phase 2: Consolidate Auth (Week 2-3)
  // Add to packages/auth/src/index.ts
  export const ROLE_HIERARCHY = ['VIEWER', 'AUDITOR', 'COMPLIANCE_OFFICER', 'ADMIN'] as const;
  export function hasRole(userRole: UserRole, requiredRole: UserRole): boolean { ... }
  export function requireAuth(minRole?: UserRole): Promise<AICSessionUser> { ... }

  Phase 3: Create @aic/middleware (Week 3-4)
  // packages/middleware/src/index.ts
  export function applySecurityHeaders(response: NextResponse): NextResponse { ... }
  export function createRateLimiter(options: RateLimitOptions): RateLimiter { ... }
  export function withTenantContext(handler: RouteHandler): RouteHandler { ... }

  Phase 4: Merge Minimal Packages (Week 4)
  - Move @aic/events, @aic/sockets, @aic/legal into @aic/types
  - Total: 47 lines consolidated into existing package

  ---
  5 Institutional-Grade Requirements We Lack

  1. Audit Trail Immutability

  Current: Hash chain in Python memory, lost on restart
  Required:
  - PostgreSQL-persisted chain with previous_hash column
  - RSA signatures stored alongside entries
  - Blockchain anchoring for regulatory proof (Ethereum/Polygon)

  2. Granular RBAC with Attribute-Based Access Control (ABAC)

  Current: 4 hardcoded roles, hierarchy checked inconsistently
  Required:
  - Permission-based system (read:audit-logs, write:incidents, etc.)
  - Resource-level permissions (user X can access org Y's data)
  - Time-limited elevated privileges
  - Segregation of duties enforcement

  3. Cryptographic Key Management

  Current: Auto-generated RSA keys, no rotation, lost on restart
  Required:
  - HSM or cloud KMS integration (AWS KMS, Azure Key Vault)
  - Key rotation schedules (90-day maximum)
  - Key usage auditing
  - Separate keys per tenant for sovereign deployments

  4. SOC 2 Type II / ISO 27001 Controls

  Current: Basic security headers
  Required:
  - Change management audit trails
  - Access review automation
  - Vulnerability scanning integration
  - Penetration test remediation tracking
  - Business continuity documentation

  5. CI/CD Enforcement with Security Gates

  Current: GitHub Actions runs tests, no security gates
  Required:
  - SAST (static analysis) blocking PRs
  - Dependency vulnerability scanning (Snyk, Dependabot)
  - Container image scanning
  - Secret detection (git-secrets, truffleHog)
  - Signed commits and deployments

  ---
  TASK 3: VISION-CODE GAP & PREDICTIVE FAILURE

  Gap Analysis: "Sovereign-Grade Audit Platform" vs Reality

  Gap 1: Audit Log is Just a Table, Not a Cryptographically Signed Ledger

  Vision: Immutable, tamper-evident audit records that can be verified by regulators
  Reality:
  -- apps/platform/db/schema.sql
  CREATE TABLE audit_logs (
      id UUID PRIMARY KEY,
      -- ... columns ...
      previous_hash VARCHAR(64),  -- Can be NULL, not enforced
      signature TEXT              -- Optional, often NULL
  );
  - previous_hash is optional, chain integrity not validated on read
  - Signatures generated but never verified in application code
  - No blockchain anchoring implemented

  Gap 2: Multi-Tenancy is Application-Level Theatre

  Vision: Bank-grade data isolation suitable for sovereign deployments
  Reality:
  - PostgreSQL RLS policies: Zero
  - Admin portal: Global access to all data by design
  - Engine API: No tenant context at all
  - One compromised admin account exposes ALL organizations

  Gap 3: Real-Time Compliance Dashboard is Polling

  Vision: Live-updating integrity scores, instant incident alerts
  Reality:
  - SSE infrastructure built but never connected
  - Dashboard polls every 30 seconds
  - No WebSocket implementation
  - Notification "system" writes to DB, doesn't push to users

  Gap 4: Rights Compliance is Mostly Hardcoded

  Vision: Automated verification of 5 Algorithmic Rights
  Reality:
  // apps/platform/app/api/dashboard/route.ts:53-75
  rightsCompliance: {
    explanation: { status: 'COMPLIANT', score: 85 },  // HARDCODED
    empathy: { status: 'COMPLIANT', score: 80 },      // HARDCODED
    correction: { status: 'COMPLIANT', score: 90 },   // HARDCODED
    truth: { status: 'COMPLIANT', score: 95 }         // HARDCODED
  }
  // Only human_agency is actually calculated

  Gap 5: Security Posture is Startup-Grade, Not Institutional

  Vision: Zero-trust architecture, defense in depth
  Reality:
  - Engine auth bypassed if PLATFORM_PUBLIC_KEY unset (main.py:70)
  - Password reset has race condition allowing token reuse (reset-password/route.ts:36)
  - User invite stores unhashed dummy password (invite/route.ts:28-33)
  - No CSRF protection on custom API routes
  - bcrypt cost factor inconsistent (10 vs 12)

  ---
  Predictive Failure Analysis: 50 Organizations Tomorrow

  First Failure: Database Connection Pool Exhaustion

  When: 50 orgs × 5 concurrent users = 250 simultaneous connections
  Where: apps/platform/lib/db.ts - default Pool(max: 10)
  Symptoms:
  - Requests hang indefinitely (no connectionTimeoutMillis)
  - Dashboard stops loading
  - Cascade failure across all tenants

  Time to Failure: Within 30 minutes of peak usage

  Second Failure: Engine API Saturation

  When: 50 orgs triggering SHAP explanations
  Where: apps/engine/app/services/explainability.py:327
  Symptoms:
  - 2-10 second blocking per request
  - 30/minute rate limit hit instantly
  - Requests queue until timeout

  Time to Failure: First day of active usage

  Third Failure: Audit Log Table Scan Degradation

  When: 50 orgs × 1000 audit entries × 30 days = 1.5M rows
  Where: Missing indexes on audit_logs(org_id, created_at)
  Evidence: Schema has idx_audit_logs_org and idx_audit_logs_created separately, not composite
  Symptoms:
  - Dashboard load time: 200ms → 5s+
  - Pagination becomes expensive

  Time to Failure: Week 3-4

  Fourth Failure: Cross-Tenant Data Exposure Incident

  When: Any admin user explores the admin portal
  Where: apps/admin/app/api/users/route.ts:13-18
  Symptoms:
  - Admin sees all users across all organizations
  - Compliance officer reports data breach
  - POPIA violation notification required

  Time to Failure: Day 1 when first organization onboards with multiple admins

  ---
  File-Level Refactor Plan: Next 10 Files
  Priority: 1
  File: apps/engine/app/main.py:70
  Change Required: Remove auth bypass when PLATFORM_PUBLIC_KEY unset
  LOE: 1h
  ────────────────────────────────────────
  Priority: 2
  File: apps/platform/db/schema.sql
  Change Required: Add RLS policies for all tenant tables
  LOE: 4h
  ────────────────────────────────────────
  Priority: 3
  File: apps/platform/lib/db.ts
  Change Required: Add pool config: max: 50, connectionTimeoutMillis: 5000, statement_timeout: 30000
  LOE: 1h
  ────────────────────────────────────────
  Priority: 4
  File: apps/admin/lib/db.ts
  Change Required: Migrate to @aic/db, remove PII logging
  LOE: 2h
  ────────────────────────────────────────
  Priority: 5
  File: apps/platform/app/api/incidents/[id]/route.ts
  Change Required: Add role check: require COMPLIANCE_OFFICER+
  LOE: 30m
  ────────────────────────────────────────
  Priority: 6
  File: apps/platform/app/api/corrections/[id]/route.ts
  Change Required: Add role check: require COMPLIANCE_OFFICER+
  LOE: 30m
  ────────────────────────────────────────
  Priority: 7
  File: apps/platform/app/api/audit-logs/route.ts
  Change Required: Add role check for POST/PATCH: require COMPLIANCE_OFFICER+
  LOE: 30m
  ────────────────────────────────────────
  Priority: 8
  File: apps/platform/app/api/auth/reset-password/route.ts
  Change Required: Make token invalidation atomic with password update
  LOE: 1h
  ────────────────────────────────────────
  Priority: 9
  File: apps/platform/app/api/users/invite/route.ts
  Change Required: Hash dummy password with bcrypt before insert
  LOE: 30m
  ────────────────────────────────────────
  Priority: 10
  File: apps/admin/app/api/users/[id]/route.ts
  Change Required: Add org_id validation before DELETE
  LOE: 1h
  ---
  Summary Risk Matrix
  ┌────────────────────┬──────────────────────┬──────────────────────┬──────────────┐
  │      Category      │    Current State     │ Series A Requirement │ Gap Severity │
  ├────────────────────┼──────────────────────┼──────────────────────┼──────────────┤
  │ Multi-Tenancy      │ App-level only       │ PostgreSQL RLS + App │ CRITICAL     │
  ├────────────────────┼──────────────────────┼──────────────────────┼──────────────┤
  │ Auth/RBAC          │ Inconsistent         │ Granular permissions │ HIGH         │
  ├────────────────────┼──────────────────────┼──────────────────────┼──────────────┤
  │ Audit Immutability │ Memory-only          │ Persisted + anchored │ HIGH         │
  ├────────────────────┼──────────────────────┼──────────────────────┼──────────────┤
  │ Scalability        │ 10 connections       │ Configurable pools   │ MEDIUM       │
  ├────────────────────┼──────────────────────┼──────────────────────┼──────────────┤
  │ Real-time          │ Polling              │ SSE/WebSocket        │ MEDIUM       │
  ├────────────────────┼──────────────────────┼──────────────────────┼──────────────┤
  │ Code Quality       │ 1,450 LOC duplicated │ Shared packages      │ LOW          │
  └────────────────────┴──────────────────────┴──────────────────────┴──────────────┘
  Investor Bottom Line: The algorithmic core (engine) is genuine. The institutional infrastructure (security, multi-tenancy,   audit immutability) is startup-grade scaffolding marketed as enterprise-ready. 4-6 weeks of focused remediation required 
  before Series A due diligence passes.
